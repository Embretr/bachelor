# Use pdflatex
$pdf_mode = 1;

# Use biber for bibliography (required for biblatex-apa)
$bibtex_use = 2;

# Enable SyncTeX for editor ↔ PDF sync
$synctex = 1;

# Output directory (keep root clean)
# $out_dir = 'build';  # uncomment if you want output in build/

# Silence most output
$quiet = 0;
