Run the write-section pipeline for section: $ARGUMENTS

Follow the orchestration instructions in `.claude/skills/write-section/SKILL.md` exactly.
Do not improvise the workflow. Stop if any required gate fails.

The argument is a section number like `2.1`, `3.2`, `4.5`, etc.
