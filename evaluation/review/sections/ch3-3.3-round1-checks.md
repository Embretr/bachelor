# Deterministic Checks — Section 3.3 Round 1

## Hard Fails

- **Placeholders**: PASS — no `[FILL IN` or `[CITATION NEEDED` markers in section text.
- **Forbidden voice**: PASS — no `we believe / we think / we found` (case-insensitive).
- **Citation keys**: PASS — no `\parencite` or `\textcite` commands in §3.3 (consistent with the BIB_SLICE constraint that §3.3 has no MUST CITE markers; cross-references to `\Cref{sec:dsr-dsrm}`, `\Cref{sec:data-analysis}`, `\Cref{sec:limitations}` only).
- **Source notes**: N/A — no MUST CITE keys for §3.3, so no source-notes verification required.
- **Compilation**: WARNING — `pdflatex` standalone succeeds (31-page `main.pdf` produced). `biber` fails with Perl `Unicode::UCD` module error (return code 25), the same local-tooling failure documented in `STATUS.md` for §3.2 round 2. Treated as tooling warning per SKILL spec convention; not a §3.3 issue.

## Warnings

- **Filler phrases**: PASS — no `it is important to note` / `it is worth noting`.
- **Repeated transitions**: PASS — Furthermore 0, Moreover 0, Additionally 0.
- **Em-dashes**: PASS — zero unicode em-dashes (`—`) in section text. All long-dash usage rendered as LaTeX `---`.
- **Section length**: WARNING — outline target ~1.5 pages (~375 words ±20% = 300–450); actual **659 words (~2.6 pages)**, +75% over target. The overshoot mirrors §3.2 round 2 (729 words at +30% over a 2-page target). The expansion is in the same places the rubric explicitly demands "every methodological choice has a 'because'" — the form-of-data-needed justification (¶1), the purposive-sample rationale (¶2), the single-day-window tradeoff (¶4), the automated-transcription rationale (¶5), and the honest-ethics-framing audit-trail (¶6). Flagged for review-agent assessment, not auto-trimmed.

## Citation Density

- **Paragraphs without citations**: ¶1, ¶2, ¶3, ¶4, ¶5, ¶6 — i.e. all six. This is by design: the outline note in §3.3 ¶1 explicitly states "no required external methodology citation here; the methodological frame is DSRM Problem Identification (Peffers, cited in §3.2), not standalone qualitative interview craft. The thematic analysis citation (§3.4 Braun & Clarke) covers what was actually done with the data." Cross-references to other sections (`\Cref{sec:dsr-dsrm}` x1, `\Cref{sec:data-analysis}` x2, `\Cref{sec:limitations}` x3) carry the deferred-citation load per the lessons-learned rule "Defer to a Ch 2 cite via cross-reference rather than re-citing the same primary-source pages downstream".

## Outline Compliance

- **Paragraph count**: outline specifies 6, section has 6 — MATCH.
- **Evidence keys (MUST EVIDENCE: research-ethics documentation, Sikt/NSD status)**: PASS — ¶6 covers Sikt status (no formal registration), informed consent (implicit, no formal form, recording without explicit pre-recording step), anonymisation (no formal protocol; only Timpex and Opter named, companies generic), data storage (local, author + supervisor only, deletion after submission), Sonix.ai cloud-processing disclosure absence. Forward references to L1, L2, L3 in `\Cref{sec:limitations}` close the loops.
