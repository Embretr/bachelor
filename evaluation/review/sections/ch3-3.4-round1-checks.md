# Deterministic Checks — Section 3.4 Round 1

## Hard Fails
- Placeholders: PASS
- Forbidden voice ("we believe" / "we think" / "we found"): PASS
- Citation keys: PASS — only `braun2006thematic` cited (5 invocations); key validated in `result/references.bib` and source notes file in Step 1c
- Source notes: PASS — `braun2006thematic` source notes file present and substantial (26575 bytes); validated in Step 1c
- Compilation: WARNING — pdflatex compiles cleanly to a 32-page main.pdf (216 KB). `biber main` returns exit code 25 — this is the persistent Unicode::UCD tooling failure first documented in §3.2 R2 and treated as a tooling warning per the SKILL spec convention; not introduced by §3.4

## Warnings
- Filler phrases ("it is important to note" / "it is worth noting"): none
- Repeated transitions (Furthermore / Moreover / Additionally): all 0
- Em-dashes (— U+2014): none found in §3.4 (HEN MARK rule satisfied; the writer used `---` for LaTeX en-dashes in prose where appropriate)
- Section length: target ~250 words (outline says ~1 page); silent INFO band 250–375 (Ch 3 +50 % rationale-per-decision calibration); actual prose ~439 words (~1.75 pages, deviation +75 % over baseline ~250, +17 % over the +50 % silent ceiling) — **WARNING**, not hard fail. The expansion sits in ¶1: the three positioning choices (inductive / semantic / essentialist-realist) each carry a "because" sentence that is required by Braun & Clarke's quality standard, and the domain-bridge sentence (psychology→transport) is required by the source's own scope caveat. ¶2 and ¶3 are tight at ~120 and ~70 prose words respectively. Trimming would either drop one positioning choice (Quality-grade flag per source notes) or drop the domain bridge (a documented quality criterion).

## Citation Density
- ¶1: 5 cites (`braun2006thematic` × 5 — base-cite + p. 87 + pp. 81–82 + Table 2 criterion 15 + closing reflexive cite). Aligned with the single MUST CITE marker.
- ¶2: 0 cites — outline has no MUST CITE marker (MoSCoW prioritisation and fit/gap analysis are project artefacts; project-internal references via `\Cref{}` to §4.1, §4.2, §4.3 satisfy the cross-reference role).
- ¶3: 0 cites — outline has no MUST CITE marker (constraints are catalogued in §5.4 via `\Cref{sec:limitations}` and the L1 / L2 forwards).

## Outline Compliance
- Paragraph count: outline specifies 3 ¶s, section has 3 ¶s — MATCH
- Evidence keys: ¶1 MUST CITE Braun & Clarke — present (`\textcite{braun2006thematic}` invocations + four `\parencite` page-locked citations). All required sources present.
