# Deterministic Checks — Section 2.1 Round 1

## Hard Fails
- Placeholders: **PASS** — no `[FILL IN]` or `[CITATION NEEDED]`.
- Forbidden voice: **PASS** — no "we believe / we think / we found".
- Citation keys: **PASS** — all 6 keys (`braekers2016vrp`, `burke2017late`, `ernst2004staff`, `glover1986future`, `pinedo2016scheduling`, `rossi2006constraint`) present in `result/references.bib`.
- Source notes: **PASS** — every cite key has a filled source notes file (≥500 bytes, contains "Notes generated from raw") at `context/docs/method/sources/raw/extracted/{bibkey}.md`. Sizes: braekers 16 kB, burke 29 kB, ernst 32 kB, glover 26 kB, pinedo 21 kB, rossi 39 kB.
- Compilation: **PASS** — `make` succeeded; `main.pdf` produced (24 pages). Two LaTeX warnings: `Reference 'sec:algorithm-design' on page 1 undefined` (×2) — forward reference to §4.5 which is not yet labelled; not a hard fail.

## Warnings
- Filler phrases: none ("it is important to note", "it is worth noting" — 0 occurrences).
- Repeated transitions: none — Furthermore=0, Moreover=0, Additionally=0.
- Em-dashes: none found in the section text.
- Section length: outline target ~2 pages (~500 words at 250 wpp); actual ~606 prose words → +21 % deviation. **WARNING (mild)** — just outside the ±20 % band. Cutting good content for length is worse than overshooting.
- Forward reference: `\Cref{sec:algorithm-design}` (×2 in ¶6) refers to a label that has not yet been created in `result/chapters/ch4/`. The §4.5 algorithm section will need `\label{sec:algorithm-design}` when it is written. Note for §4.5 writer.

## Citation Density
Per-paragraph cite commands:

| ¶ | cite-cmds | keys |
|---|----------:|------|
| 1 | 2 | pinedo2016scheduling, ernst2004staff |
| 2 | 1 | pinedo2016scheduling |
| 3 | 0 | — (intentionally interview-grounded; MUST ANCHOR Effektivitet) |
| 4 | 2 | ernst2004staff, rossi2006constraint |
| 5 | 3 | rossi2006constraint, pinedo2016scheduling, braekers2016vrp ×2 |
| 6 | 5 | pinedo2016scheduling, rossi2006constraint, glover1986future, burke2017late, ernst2004staff |

¶3 is the only paragraph without citations. This is consistent with the outline ¶-plan: ¶3 has no MUST CITE marker — only `MUST ANCHOR: Effektivitet`. The paragraph is grounded in "the interviews indicate". Reviewer may judge whether interview-grounding alone is sufficient at this scope, but the deterministic check raises no failure.

## Outline Compliance
- Paragraph count: outline specifies 6 paragraphs, section has 6 — **MATCH**.
- Evidence keys (MUST CITE / MUST ANCHOR resolution per source-fitting):
  - ¶1 MUST CITE *foundational scheduling textbook* → `pinedo2016scheduling` ✓; *adjacent-domain framing* → `ernst2004staff` ✓
  - ¶2 (no MUST CITE) — implicit Pinedo extension cite present
  - ¶3 MUST ANCHOR *Effektivitet* → present verbatim as `\textbf{Effektivitet}` ✓
  - ¶4 MUST CITE *constraint programming foundations* → `rossi2006constraint` ✓; complement `ernst2004staff` for hard/soft modelling convention ✓
  - ¶5 MUST CITE *VRP delimit (Braekers-style)* → `braekers2016vrp` ✓ (delimit-only usage; sequencing-vs-assignment separation explicit)
  - ¶6 MUST CITE *tabu-search / metaheuristic foundations* → `glover1986future` ✓ (tabu search, "meta-heuristic" term); `burke2017late` ✓ (LAHC); `ernst2004staff` for empirical heuristic-vs-CP comparison ✓; `pinedo2016scheduling` for dispatching-rule / constructive-heuristic naming ✓; `rossi2006constraint` for CP search ✓
- Anchor name validation: `Effektivitet` present verbatim in ¶3; no synonyms ("efficiency", "kontroll", "fleksibilitet", "skalerbarhet") detected.
- Trafikkoordinator placement: introduced in ¶1 last sentence ✓ (per the supervisor 2026-04-28 note: human actor early, not late).

## Summary
All hard checks pass. One mild length warning (+21 %), one warning on the forward `\Cref{sec:algorithm-design}` label that §4.5 must create. Proceed to Step 5 (review agents).