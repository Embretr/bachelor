# Naturalness Review — Section 2.1 Round 1

## Result: FAIL — Score 3/5
{"naturalness_score": 3, "ai_patterns": 8, "top_issues": ["Em-dash overuse across 4 of 5 paragraphs — strongest AI signal, fix first", "Double enumeration in paragraph 3 (hard constraints list + soft constraints list back to back)", "Filler/generic closing sentences in paragraphs 2 and 5 that add no content"], "pass": false}

## Top Issues

### 1. Em-dash overuse (paragraphs 2, 3, 4, 5)
Every use of `---` (triple-dash as em-dash parenthetical) is a strong AI signal. Specifically:
- P2: "--- a driver may be available for a time slot, but if no compatible vehicle is also available, the pair cannot be assigned" — replace with comma or full stop
- P3: "--- any solution that violates them is infeasible" — split into two sentences
- P4: "--- particularly the VRP with Time Windows (VRPTW) ---" and "--- the algorithm decides in which order locations are visited ---" — use comma or relative clause
- P5: "--- suitable for instant feedback during coordinator sessions" — split into two sentences

### 2. Double enumeration in paragraph 3
Four hard constraints listed with colons, immediately followed by four soft constraints listed with colons. Break one list into prose.

### 3. Filler closing sentences
- P2: "The practical implication is that even modest fleet sizes produce instances that cannot be solved by enumeration." → "Even a modest fleet produces instances that cannot be solved by enumeration."
- P5: "Each solver occupies a distinct position on the speed-quality tradeoff, and the multi-engine architecture allows both the coordinator and the research process to compare solver behaviour on the same problem instances." → Cut the first clause, keep only the second.
