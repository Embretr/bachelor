# Deterministic Checks — Section 3.5 Round 1

## Hard Fails
- Placeholders: PASS — no `[FILL IN` or `[CITATION NEEDED` found
- Forbidden voice: PASS — no "we believe / we think / we found"
- Citation keys: PASS (N/A) — no `\parencite` / `\textcite` calls in the section. Consistent with §3.5 having no MUST CITE markers in the outline (iteration narrative is grounded in project artefacts, not literature).
- Source notes: PASS (N/A) — no citations to validate
- Compilation: PASS (with persistent biber tooling warning per §3.2/3.3/3.4 convention) — `pdflatex main.tex` exits cleanly, produces 38-page PDF (was 32 in §3.4 R2; +6 pages from §3.5). `biber main` returns code 25 due to the Unicode::UCD Perl-module environment issue documented in earlier rounds; this is a tooling failure unrelated to §3.5 LaTeX. Section LaTeX is syntactically clean.

## Warnings
- Filler phrases: none
- Repeated transitions: none ("Furthermore", "Moreover", "Additionally" each appear < 3 times)
- Em-dashes: 8 occurrences of `---` (LaTeX em-dash). Locations:
  - line 35 (CP-SAT acronym gloss "Constraint Programming --- Satisfiability")
  - line 36 (lone trailing em-dash + clause: "time budget --- a property that matters...")
  - line 48 (lone trailing em-dash + clause: "Effektivitet --- a how-question independent of...")
  - line 61 (matched-pair em-dashes: "stale conflict displays --- the timeline showed... already resolved --- and required a refresh hook")
  - line 73 (lone em-dash + clause: "time-off windows --- week boundaries and mid-shift absences...")
  - line 87 (lone em-dash + clause: "planning rules themselves --- the soft-constraint weight space")
  - line 95 (lone em-dash + clause: "soft constraints --- workload balance, vehicle preference...")

  WARNING: count is higher than recent rounds (§3.4 R2 final: 1; §3.3 R1 final: 0). Reviewers should consider whether each is supervisor-style legitimate (acronym gloss / matched-pair fit/gap-style gloss / restatement) or convertible to colon / em-dash split / parenthetical. The acronym gloss (line 35) and the matched-pair (line 61) follow established patterns; the six lone em-dashes are conversion candidates.
- Section length: target ~1250 words (~5 pages × 250 words from outline). Actual ~1950 prose words. Deviation +56 % over baseline. WARNING — slightly above the Ch-3 silent-INFO ceiling (+50 %). Defensible by 8 iterations × "every choice has a because" rubric demand × tried/why/what-happened/learned/next pattern × actor-vocabulary ("coordinator", "Admmit", "designer-technical") repetition that the outline page-target does not absorb. Comparable to §3.3 R1 +65 % and §3.4 R2 +95 % precedents.

## Citation Density
- Paragraphs without citations: ALL paragraphs (consistent with iteration narrative grounded in project artefacts, no MUST CITE markers in §3.5 outline)

## Outline Compliance
- Paragraph count: outline specifies 1 opening framing paragraph + 8 sub-subsections (each as a six-bullet list with **Origin** first); section has 1 opening paragraph + 8 `description` environments — MATCH
- Evidence keys (anchor presence):
  - §3.5.4 MUST ANCHOR: Effektivitet → present verbatim on lines 48 and 50 ✓
  - §3.5.5 MUST ANCHOR: Tillit/kontroll → present verbatim on line 62 ✓
  - §3.5.7 MUST ANCHOR: Tillit/kontroll → present verbatim on lines 82, 84 ✓
  - §3.5.8 MUST ANCHOR: Tilpasningsdyktighet → present verbatim on lines 94, 96, 98 ✓
- Origin labels: all 8 sub-subsections lead with `\item[Origin.]` ✓
- Anchor synonym check: no `kontroll` alone, no `fleksibilitet`, no `skalerbarhet`, no `human control`, no `menneskelig overstyring`, no `operator oversight`, no `trust calibration` ✓
- Forward references: `\Cref{sec:evaluation-framework}` (§3.6, label exists), `\Cref{sec:limitations}` (§5.4, label exists in ch5 scaffold), `\Cref{sec:future-work}` (§6.3, label exists in ch6 scaffold), `\Cref{sec:hitl}` (§2.2, label exists in ch2), `\Cref{sec:dsr-dsrm}` (§3.2, label exists in ch3), `\Cref{sec:defining-task}` (§3.1, label exists in ch3) — all valid ✓
