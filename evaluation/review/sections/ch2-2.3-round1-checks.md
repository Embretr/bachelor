# Deterministic Checks — Section 2.3 Round 1

## Hard Fails
- Placeholders: PASS — no `[FILL IN` or `[CITATION NEEDED` strings
- Forbidden voice: PASS — no `we believe`, `we think`, `we found`, `we observed`
- Citation keys: PASS — `griffis2007tms` and `heinbach2022datadriven` both exist in `result/references.bib`. No keys outside BIB_SLICE used.
- Source notes: PASS — both keys validated against `context/docs/method/sources/raw/extracted/{key}.md` in Step 1c (both ≥500 bytes, contain "Notes generated from raw").
- Compilation: PASS — `make` exit 0; 28-page `main.pdf` written. Two undefined `sec:algorithm-design` references are pre-existing in §2.1 (not introduced by §2.3).

## Warnings
- Filler phrases: none
- Repeated transitions: Furthermore=0, Moreover=0, Additionally=0
- Em-dashes: none in §2.3
- Section length: target ~250 words (~1 page); actual 399 words; deviation +60 % → WARNING (not hard fail). Length over-run is driven by the supervisor calibration's three named requirements per paragraph (definition + scope-lock; named Norwegian + international vendor + variable-adoption framing; gap statement with two literature-anchored claims). Reviewer may decide whether to trim or accept.

## Citation Density
- ¶1: 4 citations (heinbach2022datadriven ×2, griffis2007tms ×2 incl. \textcite)
- ¶2: 4 citations (heinbach2022datadriven ×2 incl. \textcite, griffis2007tms ×2 incl. \textcite)
- ¶3: 4 citations (griffis2007tms ×2, heinbach2022datadriven ×2 incl. \textcite each)
- All paragraphs have citations; no zero-citation paragraphs.

## Outline Compliance
- Paragraph count: outline specifies 3, section has 3 — MATCH
- Evidence keys:
  - ¶1 MUST CITE "TMS as software category (definitional)" — satisfied by `heinbach2022datadriven` p. 811 (Gartner-derived definition) and `griffis2007tms` p. 19. ✓
  - ¶2 MUST CITE "international TMS vendor reference (literature-anchored)" — satisfied by `heinbach2022datadriven` p. 819 (TRUCKAHEAD as named European workflow platform) and `griffis2007tms` p. 26 (45 North American firms; 50 % manual). ✓
  - All required sources present.

## Supervisor calibration check (deterministic)
- Definition unchanged across ¶1→¶3: ¶1 locks "planning and execution across order management, route handling, carrier coordination, and freight billing"; ¶2 and ¶3 do not narrow it. PASS.
- Vendors named: Timpex ✓, Opter ✓, TRUCKAHEAD ✓ (international, literature-anchored). No Trimtex/Opptur (forbidden synonyms) — `grep -niE "trimtex|opptur" /tmp/sec23.tex` empty.
- "Neither generates assignment plans automatically" — present verbatim in ¶2.
- Variable adoption framing: ¶2 closes with "running a TMS is not universal, and several transport companies operate without one." PASS.
- No interview material: no "interview", "intervju", or coordinator quotes in §2.3 prose. PASS.
- Bridge sentences: ¶1→¶2 ("Within that scope, the live state-of-the-art is uneven across vendors and across the sector."); ¶2→¶3 ("The systems just named occupy a recognisable slice of the TMS scope, but they leave a gap inside it."). PASS.
- "fit/gap" not in prose: confirmed absent from §2.3 (`grep -i "fit/gap" /tmp/sec23.tex` empty). ¶3 reframes as "what existing systems cover" / "what is missing". PASS.

## Status
All hard checks PASS. Length warning logged but does not gate. Proceeding to Step 5 (review agents).
